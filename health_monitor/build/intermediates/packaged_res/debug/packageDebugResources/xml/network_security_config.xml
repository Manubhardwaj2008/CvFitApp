<?xml version="1.0" encoding="utf-8"?>
<!--
  The FastAPI backend runs as plain HTTP on your dev machine, so we allow
  cleartext (non-HTTPS) traffic only to the hosts you'd actually use during
  local development:
    - 10.0.2.2      -> the Android EMULATOR's alias for your host machine's localhost
    - localhost/127.0.0.1 -> a physical device connected via adb reverse
    - 192.168.0.0/16 and 10.0.0.0/8 -> typical local LAN ranges for a phone on the same Wi-Fi

  If you deploy the backend somewhere with a real domain + HTTPS, remove this
  file and the android:usesCleartextTraffic attribute in the manifest.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
